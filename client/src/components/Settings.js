import { Component } from "react";

export default class Settings extends Component {
    render() {
        return (
            <div>
                HELLO SETTINGS
                {this.user}
                {this.userObject}
            </div>
        )
    }
}